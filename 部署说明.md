# 网址版部署说明

把论文降重助手部署到公网，得到一个 **https 网址**，任何设备浏览器打开即用（无需安装）。

## 方案一：Netlify（推荐，免费，AI 改写也能用）

1. 打开 https://app.netlify.com/drop （无需注册即可试用；注册账号可长期保留）
2. 把整个 `paper-dedup` 文件夹**直接拖进**页面
3. 几秒后 Netlify 会给出一个 `https://xxxx.netlify.app` 网址，完成！

- 已内置 `netlify.toml` 与云函数 `netlify/functions/api-chat.mjs`，**AI 改写模式在公网同样可用**（DeepSeek API Key 仍需用户自己填写）。
- 想绑定自己的域名：Netlify 后台 → Domain settings → Add custom domain。

## 方案二：Vercel（免费）

1. 注册/登录 https://vercel.com
2. New Project → Import 这个文件夹（或使用 vercel CLI：`vercel --prod`）
3. 自动部署，得到 `https://xxxx.vercel.app`

> Vercel 上 AI 改写模式会因缺少 /api/chat 代理而不可用（除非自行添加 Serverless Function），其余功能完全正常。

## 方案三：GitHub Pages / Gitee Pages / 任意静态托管

把 `index.html`、`css/`、`js/` 三个部分上传即可。

- 纯静态托管**没有 /api/chat 代理**：AI 改写模式受浏览器跨域限制，可能不可用；**离线降重、查重估算、知识库全部正常**。
- 示例（GitHub Pages）：
  1. 新建仓库 → 上传 `index.html`、`css/`、`js/`
  2. Settings → Pages → 选择分支 → 保存
  3. 得到 `https://用户名.github.io/仓库名/`

## 局域网版（无需公网）

- 双击 `start-lan.bat`，同一 WiFi 下的手机/平板/其他电脑访问 `http://本机IP:8642`（脚本会自动打印 IP）。
- 首次运行 Windows 防火墙弹窗请选"允许访问"。
- 注意：局域网版要求运行服务的那台电脑保持开机，且 DeepSeek 调用走那台电脑的网络。

## 隐私提示

- 纯静态部署时，**所有数据都在访问者的浏览器里处理**，不上传任何服务器。
- AI 改写模式下，论文文本会发送给 DeepSeek 官方 API（这是使用 AI 功能的必要条件），请勿在含涉密内容的文档上使用。
